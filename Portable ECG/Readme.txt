# Uses latest Python version 3.2.6
# The code has the name of the sample file name "100.txt" as a parameter input.
# Requires numpy and pyqtgraph as the libraries for smooth and interactive graph implementation.
